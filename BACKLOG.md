# Backlog

Everything deferred, and why.

**Scope is frozen per release.** Adding a line here is free. Building it
mid-release is not. Anything that arrives mid-release becomes a line here and is
considered at the next boundary.

Without that rule this file is a wishlist. With it, it is the pressure release
valve that lets you say "not now" without losing the idea — which is what
actually stops mid-release scope reversal.

## Two kinds of row

**`**Unmet:**` rows** name a clause the plan promises and the code does not do.
They are load-bearing: `scripts/audit-status.mjs` reads them, and a slice may
wear `[!]` only while one of these names its open clause. Start the Item cell
with the literal text `**Unmet:**` — the audit matches that prefix exactly.

To release the tick when the clause is finally built, strike the row through or
annotate it as closed. Do not delete it: a struck row records that the gap
existed and was closed; a deleted one is a gap that was never admitted.

**Every other row** is ordinary deferred work. It holds nothing back and never
blocks a tick.

The **Consider at** column names the slice this is FILED AGAINST — the one it
blocks, not the one that will eventually clear it.

## Open clauses

| Item | Raised | Consider at | Note |
| ---- | ------ | ----------- | ---- |
| ~~**Unmet:** R2.1 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.1 | **Closed 2026-09-09.** Inspected in Bengali at all three widths and in English at 375px. The warning banner, eight-tile KPI grid, quick links and recent-activity card stayed inside the viewport; the 980/375px bottom tab bar left the page end unobscured. No horizontal overflow at any width. |
| ~~**Unmet:** R2.1 session-end verification — 7 auth-cookie web tests fail before fetch~~ | 2026-09-09 | R2.1 | **Closed 2026-09-09.** Node's native `Request` rejects jsdom's `AbortSignal`; the timeout method itself exists. The shared test setup now uses Node's matching abort classes. All 17 affected auth tests pass with production auth and its four-second timeout unchanged. |
| ~~**Unmet:** R2.2 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.2 | **CLOSED 2026-09-10.** Viewed the roster, bulk bar and both confirmations at all three widths using synthetic local users. Fixed long-name overflow in dialogs/table and narrow phone bulk labels; confirmed Bengali/English, self-selection refusal, search/empty results and safe cancellation. Added regressions for translated controls, containment classes, loading/error/denied states and hidden selections after search. Table-only horizontal scrolling remains intentional; no account deletion was submitted through the browser. |
| ~~**Unmet:** R2.3 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.3 | **CLOSED 2026-09-10.** Viewed identity, eight summary cells and all four populated tabs at 1440 / 980 / 375px with synthetic local records; checked Bengali and English, empty self-inspector, grant sources and back navigation. Fixed long-email/name/budget overflow, untranslated group/frequency/date and incorrect paused label. Phone revoke/suspend/delete dialogs fit and cancel safely; no moderation action submitted. Regression tests cover partial fetch failures, stale identity after route changes, failed clipboard writes, grant combinations and denied access. |
| ~~**Unmet:** R2.4 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.4 | **CLOSED 2026-09-11.** Viewed the twelve-month chart/table, group/category/payment panels, debt tiles and top spenders at all three widths using synthetic local records; Bengali and English checked. Fixed 932px phone overflow from long ranked entries, raw group/payment codes, nonzero bars for zero amounts and sub-taka scaling. Labels now distinguish the rolling trend, all-time rankings, relative bars and recorded debt amounts. Seven UI and two API regressions cover layout/localization, zero/fractional values, loading/empty/error/denied states, window boundaries and settled debt semantics. |
| ~~**Unmet:** R2.5 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.5 | **CLOSED 2026-09-11.** Viewed the taxonomy table, share bars, drift warning banner and merge modal at 1440 / 980 / 375px. Fixed success timer cleanup on unmount, long category break-words wrapping, localized group display, and added accessible button labels. Automated tests cover timer cancellation, localized groups, long-name modal and table containment, empty and error states. |
| ~~**Unmet:** R2.6 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.6 | **CLOSED 2026-09-11.** Viewed the export card, CSV dropzone and import preview table at 1440 / 980 / 375px. Fixed hardcoded English password column header with bn/en localization (`adminUserPassword`), added auto-dismissing timer cleanup on unmount for the success message, added `aria-label` attributes to the dropzone and sample download triggers, and constrained preview cells (`truncate`, `max-w-*`, `overflow-auto`) to protect phone viewports. Automated regression tests verify timer cancellation, preview truncation/localization in bn and en, invalid CSV error banner, and export failure handling. |
| ~~**Unmet:** R2.7 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.7 | **CLOSED 2026-09-11.** Viewed the audit log table, actions filter select, search input, and pagination controls at 1440 / 980 / 375px with synthetic records. Staved off mobile header blowout by adopting full-width responsive stacking (`w-full sm:w-auto`) for the filter dropdown and search input at 375px. Added explicit `aria-label` attributes to the action filter, search input, target user inspect link, and pagination arrow buttons. Added desktop hover titles to truncated actor, target and detail table cells. Regression tests in `tests/admin-screen.test.tsx` verify header/pager localization in bn and en, empty audit log state, and API error banners. |
| ~~**Unmet:** R2.8 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.8 | **CLOSED 2026-09-11.** Viewed the superadmin roster with grant-source indicators, environment email panel, candidate list, and grant/revoke confirmation modal at 1440 / 980 / 375px. Converted dangling success timeout to an unmount-safe `useEffect` cleanup hook. Constrained long identity names and email strings with `break-words` and `truncate` to protect 375px phone layouts and confirmation dialogs. Added accessible button `aria-label` attributes distinguishing targeted users, full-width responsive candidate search input (`w-full sm:w-auto`), and enforced self-revocation refusal. Automated regression tests verify timer cleanup on unmount, localization and self-revocation refusal across bn and en, error banners, and modal cancellation. |
| ~~**Unmet:** R2.9 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.9 | **CLOSED 2026-09-11.** Viewed the session table, stat tiles, KV ephemeral warning banner, and revoke confirmation modal at 1440 / 980 / 375px. Converted dangling 4-second `setTimeout` in the revoke callback to an unmount-safe `useEffect` cleanup hook, preventing state updates on unmounted component. Name cells use `break-words` instead of `whitespace-nowrap` to protect 375px phone viewports. Truncated email cells now carry a `title` attribute for hover inspection. Revoke buttons carry accessible `aria-label` attributes distinguishing the target user. Modal user name uses `break-words` to prevent dialog overflow. Regression tests verify timer cleanup on unmount, localization in bn and en, empty session state, API error banner, and modal cancellation. |
| ~~**Unmet:** R2.10 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.10 | **CLOSED 2026-09-11.** Viewed the warning list, healthy state banner, database card, session store card, and configuration card at 1440 / 980 / 375px. The `Row` component already uses `break-all` on values, preventing overflow at 375px for CORS origins and superadmin email lists. Technical labels (Dialect, Backend, Mode, Version) are kept in English since they mirror environment variable names and technical identifiers. Regression tests verify KV fallback warnings with localised headers in bn and en, healthy banner when warnings are empty, and API error banner. |
| **Unmet:** R3.1 Done — never run against a real spreadsheet | 2026-09-09 | R3.1 | Every test drives a mocked Sheets API, so the contract is pinned but the round trip is not: the service-account share, the tab-title match against real Bengali sheet names, and how Google parses an ISO date and a decimal string under USER_ENTERED in the owner's locale are all unverified. Clearing it is one sync against a copy of the workbook, checking that the month's total is unchanged after syncing twice. Cost of leaving it: the first real sync could fail on a detail no unit test can see. |
| ~~**Unmet:** R2.11 DoD 1 — not viewed at 1440 / 980 / 375px~~ | 2026-09-09 | R2.11 | **CLOSED 2026-09-11.** Viewed the Sheets sync card, voice parser card, user count card, and status pills at 1440 / 980 / 375px. Service account email addresses and credential source values already employ `break-all` on monospace and English spans to prevent horizontal overflow on 375px viewports. The status pill renders cleanly across mobile breakpoints with localized text in bn and en. Automated regression tests verify deployment service account reporting without secret leaking, card and badge localization in bn and en, the not-configured status pill with details when Sheets credentials are missing, and API error banner handling. |

| **Unmet:** R3.2 Done — never run against a real spreadsheet | 2026-09-09 | R3.2 | The three ledger tabs are proven twice on paper and not once in practice: every formula was recalculated in LibreOffice against sample rows, and every write is pinned by a mocked-Google test. What no mock can show is the round trip — whether Google's tab-title match accepts `ধার-দেনা` and `পুনরাবৃত্ত খরচ` exactly as written, whether a `USER_ENTERED` write is *accepted and flagged* rather than rejected by a cell whose data validation is set to reject invalid input (this code assumes accepted-and-flagged), and how the settle date and next-run date parse in the owner's spreadsheet locale. Clearing it is one full sync against a copy of the new template, checking that the debt panel's totals match the app's ধার-দেনা screen and that a second sync changes nothing. Cost of leaving it: the first real sync could fail on a detail no unit test can see. |

| **Unmet:** R3.3 Done — multi-year rollover has not run against Google | 2026-09-09 | R3.3 | Code and mocked request contracts cover twelve new months, a separate summary with retargeted charts, leap-day validation and budget picker expansion. The owner chose separate yearly summaries. Still required: use an explicitly approved disposable workbook, sync 2027 and leap-year February 2028, inspect native formulas/charts/date validation and both years' totals, then repeat to prove no duplication. Verify a values-write failure followed by retry leaves no copied source expenses. No real Google workbook was written in this session. |

## Deferred work

| Item | Raised | Consider at | Note |
| ---- | ------ | ----------- | ---- |
| ~~The sheet covers one year; a month outside it refuses the whole sync~~ | 2026-09-09 | R3.3 | Implemented on the owner's next-task request, with separate summaries per year explicitly selected. Intact 2026 templates now support automatic whole-year preparation. Live verification remains a separate named open clause above. |
| The sheet can hold the ledger but not reproduce the app's behaviour | 2026-09-09 | — | Recorded so the fallback is not mistaken for a replacement. After R3.2 the sheet holds every record type the app does, but recurring rules do not materialize themselves there, a debt's partial payment has to be typed into the বাকি cell by hand, and nothing enforces the app's own validation on a row typed straight into the sheet. `GET /export/backup.json` stays the only full-fidelity, restorable copy. |
| ~~Version labels disagree across shipped surfaces~~ | 2026-09-09 | R3.5 | **CLOSED 2026-09-10.** Shared UI, Expo and package metadata now agree with the existing web/API release `0.28.0`. A tested eleven-surface audit in `pnpm verify` rejects drift or missing metadata. The web chip/footer were viewed at 1440 / 980 / 375px, with Bengali/English interaction; mobile metadata is audited and typechecked. Explicit API runtime overrides remain valid. No native build-number bump or app-store release. |
| ~~No CI: `pnpm verify` runs only on a developer machine~~ | 2026-09-09 | R3.4 | **CLOSED 2026-09-09.** The Verify workflow runs on pushes, pull requests and manual dispatch, with locked installs, contract-drift checks, the complete verification gate and a production build on Linux and Windows. [First hosted run](https://github.com/bestdeveloper2u/poipoihisab/actions/runs/34384774609) passed both jobs. No production secrets or deployment steps; required merge checks remain an owner-controlled policy decision. |
| Retro-verify R1 against this Definition of Done | 2026-09-09 | R3 | R1's twelve boxes read `[x]` because the slices are shipped, in production, and each has a Done clause a test already proves. They were never walked against points 1–4 of the current DoD, which did not exist when they were built. Not an open clause — the plan makes no promise R1 breaks — but the ticks are softer than R2's will be once its eleven rows above are cleared. |
| Migrate to Vercel Services rather than the `api/index.py` shim | 2026-09-09 | R3 | Closed decision D2 keeps the API same-origin, and Services gets that with separate per-service builds, so a CSS change stops rebuilding the Python function. Costs: the shim goes, `functions.regions` moves into the service block, and the feature needs the Services permission on the plan. Not urgent — the coupling costs build minutes, not correctness. |
| `/admin/security` scans at most 300 profiles, one KV round trip each | 2026-09-09 | R3 | The per-user session index has no reverse mapping, so listing live sessions across the platform is O(users) lookups. Fine at current scale and it will quietly stop being the whole truth as the user count grows — the page reports how many it scanned, which is what keeps it honest rather than wrong. A reverse index would fix it and is not worth a schema change yet. |
| Category merge has no undo | 2026-09-09 | R3 | `POST /admin/categories/merge` rewrites `expenses.cat` for every user in one statement. The audit row records the from/to pair and the affected count, so the merge is reconstructible by hand, but there is no one-click reverse. Deliberate for now: the reverse of a merge is ambiguous once two source values have been folded into one. |
| The mobile app has no admin surface | 2026-09-09 | — | Intentional, recorded so it is not mistaken for an omission. D5 makes superadmin an operator role, and operators work from a desktop. `apps/mobile` carries the member screens only. |
| Confirm the Supabase project region matches the API's pinned Vercel region | 2026-09-09 | R3 | The region is `functions["api/index.py"].regions` in `vercel.json` and it has already moved once (Seoul to Mumbai) since this row was written, so the row names the source rather than the value — a figure copied into prose goes stale the first time somebody tunes it. If the database sits elsewhere, round-trip latency dominates the pooler and NullPool work already done. One console check, filed because it is an infrastructure fact nobody has written down. |
