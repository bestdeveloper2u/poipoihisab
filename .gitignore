node_modules/
dist/
coverage/
.vite/
.expo/
*.tsbuildinfo
*.log
.env
.env.local

# python (apps/api)
.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.db
*.sqlite3
.vercel
.env*
# ...but the example MUST be committed: scripts/audit-env.mjs reads it as the
# declared side of the both-directions check, and a fresh clone reads it to
# know what to set. `.env*` above had been quietly swallowing it.
!.env.example
